namespace DM2Projekt.Models;

// used in Room
public enum RoomType { Classroom, MeetingRoom }
public enum Floor { Ground, First, Second, Third }
public enum Building { A, B, C, D }

// used in User
public enum Role { Student, Teacher, Admin }
