@page
@model DM2Projekt.Pages.Smartboards.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Smartboard[0].IsAvailable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Smartboard[0].Room)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Smartboard) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsAvailable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Room.RoomId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SmartboardId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SmartboardId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SmartboardId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
