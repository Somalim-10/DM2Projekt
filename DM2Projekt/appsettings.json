{
  // logging stuff, nothing crazy
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // let any host hit this app (cool for dev stuff)
  "AllowedHosts": "*",

  // NOT the real connection string — just a dummy placeholder
  "ConnectionStrings": {
    "DM2ProjektContext": "yo-load-this-from-appsettings.Local.json"
  },

  // email config — empty 'cause secrets live elsewhere
  "Email": {
    "SmtpHost": "",
    "SmtpPort": "",
    "SmtpUser": "",
    "SmtpPass": "",
    "FromEmail": "",
    "FromName": "Zealand Booking Bot" // this one’s fine to keep
  }
}
